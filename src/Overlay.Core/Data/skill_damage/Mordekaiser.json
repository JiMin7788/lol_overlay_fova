{
  "_note": "Curated (T3.3). P (Darkness Rise): while active, an on-hit MAGIC aura ticks bonus damage to nearby enemies (AuraDamagePerStack = 5 + 0.30 AP, BIN-sourced from MordekaiserPassive.mSpellCalculations). Curated as a P bonusEffect trigger onHit so it is added to each auto-attack in a combo — an assistive approximation of the DoT aura (the aura ticks per second, not strictly per hit; documented). Q/E corrected to MAGIC (Mordekaiser deals magic damage; the auto-picker mis-tagged Q Physical). W (shield/heal) and R (stat-steal realm) carry no combo damage calc, so they are omitted. Kayn/Rhaast passive was NOT curated: its damage lives in a tooltipOnly, obfuscated calc part the interpreter cannot resolve, and the rest is healing — left for the manual sub-icon path.",
  "P": { "bonusEffects": [ { "trigger": "onHit", "hits": [ { "type": "Magic", "calc": "AuraDamagePerStack", "count": 1 } ] } ] },
  "_noteQ": "Q (Obliterate) — M25 §11.G VsIsolated conditional (loop 106). BIN 'QDamage' = base magic (grouped target). 'EmpoweredDamageTooltip' = the ISOLATED value = GameCalculationModified(QDamage x IsolationScalar), the scalar a BIN DataValue (so BIN-sourced, not hardcoded) — Morde Q deals bonus when the target has no nearby allied champions. Isolation is NOT exposed by the Live Client API -> VsIsolated is UserAssumed: calc:QDamage is the UNMET anchor (grouped = conservative RangeMin, preserves the prior resolved number), metCalc:EmpoweredDamageTooltip is the MET/isolated end (RangeMax). The UserConditionMet knob defaults unmet; the exposure-graph spans [grouped, isolated]. (The earlier 'MordekaiserIsolatedQ' name lives in a sibling tooltip spell the parser's Q slot doesn't expose; EmpoweredDamageTooltip is the resolvable one, right in the Q spell.)",
  "Q": { "hits": [ { "type": "Magic", "calc": "QDamage", "conditionType": "VsIsolated", "metCalc": "EmpoweredDamageTooltip", "count": 1 } ] },
  "E": { "hits": [ { "type": "Magic", "calc": "TotalDamage", "count": 1 } ] },
  "_noteW": "W (Indestructible): a shield/heal (grey-health absorb then heal), zero damage calcs in the BIN. No hits."
}
