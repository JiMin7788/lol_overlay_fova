{
  "_note": "Curated (T2c + T3.x passive pass). Q (Icathian Rain) fires 6 missiles; MaxDamageTotal already sums all missiles on one target, so it is used with count 1 instead of the per-missile tooltip calc the auto pass picked. W base damage is EffectValueCalculationPart (not evaluable), E/R are utility/shield. P (Second Skin/Plasma): basic attacks apply on-hit MAGIC damage (PBaseDamage = level-interpolated base 4→24 + 0.12x AP, KaisaPassive.mSpellCalculations). Curated as a P bonusEffect trigger onHit; the additional per-stack scaling term (PCurrentPerStackDamage) is intentionally omitted as a conservative approximation (only the flat base proc is modeled, not the stack ramp). CORRECTED loop 219: W (Void Seeker) was previously left uncurated with the stated reason 'W base damage is EffectValueCalculationPart (not evaluable)'. That reasoning is disproven by this codebase's own Evelynn R (Last Caress), which already successfully curates via calc:'CritDamage' -> GameCalculationModified -> 'Damage', and 'Damage' itself is EffectValueCalculationPart(mEffectIndex=1) + StatByCoefficient(0.75) -- the identical shape to Kaisa W's 'TotalDamage' (EffectValueCalculationPart(mEffectIndex=1) + StatByCoefficient(mStat=2 TOTAL AD, 1.3) + StatByCoefficient(no mStat -> AP, 0.45)). The engine resolves a named calc's formula tree generically including EffectValueCalculationPart (it reads the SAME spell node's mEffectAmount array by index) -- this is unrelated to the genuinely-unsupported BuffCounterByNamedDataValueCalculationPart/BuffCounterByCoefficientCalculationPart class (live buff-stack state, e.g. this champion's own P per-stack term, still correctly left uncurated). Independently corroborated against this session's pre-collected Kaisa web evidence: wiki quote '30/55/80/105/130 (+130% AD)(+45% AP)' matches TotalDamage's formula (BIN mEffectAmount#1 = [30,55,80,105,130] for ranks 1-5, StatByCoefficient AD=1.3 total AD, AP=0.45) exactly, digit for digit -- confirms TotalDamage is the real live formula, not a tooltip-only approximation, despite its 'tooltipOnly': true flag (that flag is UI metadata only; grepped for it across the engine and it is never read/branched on, same non-finding as AurelionSol's MaxDamageTooltip false-positive-filter case this session). W curated as a single Magic hit, calc:'TotalDamage', count 1. [M32 pilot-crosscheck follow-up ADJUDICATED 2026-07-24: the follow-up claim that Q secondary-missile 25% values are NOT_IN_BIN is CLOSED - they are composed INSIDE the curated MaxDamageTotal calc via the ExtraHitReduction DataValue (BIN calc tree: BaseDamage+BonusADRatio+APRatio twice, second branch scaled by ExtraHitReduction). Single-target full-volley total already curated; no change needed. Evidence: curation_evidence/Kaisa.json.]",
  "P": {
    "bonusEffects": [
      {
        "trigger": "onHit",
        "hits": [
          {
            "type": "Magic",
            "calc": "PBaseDamage",
            "count": 1
          }
        ]
      }
    ]
  },
  "Q": {
    "hits": [
      {
        "type": "Physical",
        "calc": "MaxDamageTotal",
        "count": 1
      }
    ]
  },
  "W": {
    "hits": [
      {
        "type": "Magic",
        "calc": "TotalDamage",
        "count": 1
      }
    ]
  },
  "_noteSweep2": "STAGE-4 AUDIT (loop 441, 2026-07-27), NO calc changes: the four Q MaxDamage* DataValue families (base/Evolved x NL variants) are TOOLTIP MIRRORS of the SubParts composition the curated MaxDamageTotal already computes; the EVOLVED-Q variant itself stays uncurated because evolution state is not visible in the Live Client API (conservative base-form floor). P PDamageCap(400) = the plasma-detonation monster cap, out of the champion-damage scope.",
  "_noteSweepTriage": "SWEEP TRIAGE name ledger (loop 442): MaxDamage / MaxDamageNL / MaxDamageEvolved / MaxDamageEvolvedNL are the four tooltip-mirror families adjudicated in _noteSweep2 (evolved state not API-visible; NL = no-lockon variant)."
}
