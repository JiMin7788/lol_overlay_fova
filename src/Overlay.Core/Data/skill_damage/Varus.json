{
  "_note": "Hand-curated this pass (loop 39), cross-verified against BIN mSpellCalculations/mDataValues plus a live leagueoflegends.fandom.com/wiki/Varus fetch (2026-07-10; wiki.leagueoflegends.com/en-us/Varus itself returned an empty JS-rendered body) for mechanics/types. P (Living Vengeance) omitted entirely: confirmed via BIN that VarusPassiveAbility/VarusPassive carries ONLY attack-speed-buff calculations (ChampionAS/MinionAS/ASDuration/ChampionAD/ChampionAP -- all AS or on-hit adaptive-force conversions, no damage calc anywhere) -- genuinely no-damage passive matching Riot's own kit description ('gains bonus attack speed on takedown'), so no P slot key is written at all (not an empty {} placeholder). All Q/W/E/R slots hand-verified; auto flag removed.",
  "_noteQ": "Piercing Arrow. BIN TotalDamageMax = NamedDataValue(BaseDamageMax) + StatByNamedDataValue(mStat=2, mStatFormula=2 => BONUS AD, tADRatioMax=1.2). This is the FULLY-CHARGED (max channel time) single-target value -- the wiki's own 'Maximum Physical Damage' tooltip line. A separate TotalDamageMinTooltip / {b3ef3984} calc (= TotalDamageMax x ChargeMultiplierInverse=0.667) represents the MINIMUM (instant-recast, no charge) value; not curated here, matching this project's convention of modeling the fully-realized/landed version of an ability (same spirit as Darius/Garen's precedent of picking the informative primary number) -- a combo-tool user charging Q to max before releasing is the intended default assumption. FalloffPercent (AoE multi-target damage reduction) is not modeled -- this schema is single-target. Tagged BurstCeiling: fully-charged Piercing Arrow (further amplified by its cosmetic-crit display and typical lethality/crit itemization) is Varus's single highest realistic burst number, distinct from his sustained on-hit E/R kit. Deliberately NOT tagged Stacking -- charge-time scaling is a windup mechanic, not a repeated-application stack mechanic (Blight is the real stacking mechanic, curated under W below).",
  "Q": { "hits": [ { "type": "Physical", "calc": "TotalDamageMax", "minCalc": "TotalDamageMinTooltip", "maxCalc": "TotalDamageMax", "count": 1 } ], "tags": ["BurstCeiling"], "_noteQ": "M24 P3 ChargeHold/DistanceScaled (loop 102): Piercing Arrow ramps with charge. calc:TotalDamageMax kept as resolved default (full-charge = value identity, zero regression); range [TotalDamageMinTooltip(uncharged), TotalDamageMax(full)] via UserDistanceFraction (charge). NOTE: TotalDamage resolves to the same as TotalDamageMax, so TotalDamageMinTooltip (a GameCalculationModified the FormulaInterpreter can resolve) is the real min-charge end." },
  "_noteW": "Blighted Quiver. Two BIN-confirmed magic-damage components, both curated as W bonusEffects (the calcs live in VarusWAbility/VarusW's mSpellCalculations; W itself has no direct 'cast' damage of its own -- it is a toggle/on-hit passive plus an active-empower utility, so no 'hits' key is written for W, only bonusEffects). (1) OnHitDamage (trigger onHit, fires on every basic attack): NamedDataValue(VarusWOnHitDamage) + StatByNamedDataValue(no mStat => AP, OnHitRatio=0.25) + StatByNamedDataValue(mStat=2, mStatFormula=2 => BONUS AD, OnHitADRatio=0.15) -- the live BIN carries a bonus-AD term (OnHitADRatio) the fandom mirror's older text ('7-11 + 25% AP') does not mention; the BIN (current patch) supersedes the wiki text as ground truth per the dynamic-lookup hard rule, and only the calc name/kind are curated here so it resolves live regardless of which patch changed the numbers. (2) MaxPercentHPPerStack (trigger onAbility, tagged Stacking + PercentMaxHp): ModifiedGameCalculation = PercentHPPerStack x3, i.e. the FULL 3-Blight-stack detonation magic damage Varus's OTHER abilities (Q/E/R) deal when they land on a target already carrying 3 Blight stacks (stacks applied by this same W's on-hit passive, or by R's full-stack application) -- 'Varus' other abilities consume stacks of Blight on enemies hit to deal bonus magic damage' per the wiki. HONESTY CAVEAT: modeled as onAbility (fires on every Q/W/E/R node per this project's existing BonusTrigger semantics), which (a) assumes the target already carries the full 3 stacks -- an upper-bound assumption, since stack count is not live-tracked by this engine -- and (b) will also technically fire on a W node itself even though W is the stack-APPLIER, not a consumer -- a known imprecision of the onAbility trigger's all-or-nothing scope, flagged here rather than silently accepted. Deliberately NOT curated: the separate Q-specific 'next Piercing Arrow deals bonus missing-HP magic damage' active-empower term (WQHealthDamage / QEmpowerPercentHP / MaxQEmpowerPercentHP) -- this only applies when the player manually activates Blighted Quiver's toggle immediately before a specific Q cast, a timing/state condition this static per-slot bonusEffect schema cannot express without either double-counting the already-curated Blight-consumption bonus or conflating two distinct triggers into one; left unresolved rather than fabricated.",
  "W": { "bonusEffects": [
    { "trigger": "onHit", "alwaysOn": true, "hits": [ { "type": "Magic", "calc": "OnHitDamage", "count": 1 } ] },
    { "trigger": "onAbility", "hits": [ { "type": "Magic", "hpPercentCalc": "MaxPercentHPPerStack", "hpBasis": "Max", "count": 1, "tags": ["Stacking", "PercentMaxHp"] } ] }
  ] },
  "_noteE": "Hail of Arrows. BIN TotalDamage = NamedDataValue(BaseDamage) + StatByNamedDataValue(mStat=2, mStatFormula=2 => BONUS AD, BonusADRatio=0.9) -- matches the wiki's 'Physical Damage ... (+ 60% bonus AD)' shape (flat + bonus-AD physical; the current BIN ratio differs slightly from the fandom mirror's older number, again resolved live so not a concern per the dynamic-lookup rule). Single on-impact hit -- the ground zone lingers only for its slow/Grievous-Wounds debuff, not a separate damage-over-time, so no perSecondHpPercent shape is needed.",
  "E": { "hits": [ { "type": "Physical", "calc": "TotalDamage", "count": 1 } ] },
  "_noteR": "Chain of Corruption. BIN TotalDamage = NamedDataValue(BaseDamage) + StatByCoefficient(no mStat => AP, coefficient=1.0 = 100% AP) -- matches the wiki's 'Magic Damage ... (+ 100% AP)' shape exactly. Single hit on the first champion struck (the tendril then seeks additional nearby champions to root/Blight, but the wiki does not describe additional TotalDamage instances beyond one direct hit per infected target, so count stays 1 per target in this single-target combo model). R also applies full (3) Blight stacks over 1.5s to its target -- this is Blight APPLICATION, not consumption, so it is not itself an additional damage instance; the W-curated onAbility Blight-consumption bonus above already fires alongside this R cast per this project's onAbility semantics, covering the case where R also detonates a pre-existing mark.",
  "R": { "hits": [ { "type": "Magic", "calc": "TotalDamage", "count": 1 } ], "tags": ["AoeUltimate"] },
  "_noteSweepTriage": "SWEEP TRIAGE (loop 442, 2026-07-27): Q MinDamagePercent(0.33) is the uncharged floor of the charge ramp - Varus is on the golden-locked list (coverage row 7 owns the min/max-charge lock); flag recorded for that session, no change here.",
  "_noteSweepTriage2": "SWEEP TRIAGE 2 (loop 442): W VarusWMaxChargeHPDamage(1.5) belongs to the blight/charge family owned by the golden coverage row 7 session. Documented pointer."
}
